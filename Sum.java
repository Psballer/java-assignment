import java.util.Scanner;
    public class Sum{
        public static void main(String[] args){
            Scanner input = new Scanner(System.in);
            System.out.println("Enter the subtotal and a gravity rate: ");
            double subtotal = input.nextDouble();
            double gravityrate = input.nextDouble();
            double symbol;
            double gravity = Symbol + gravityrate/100;
            double total = Symbol + gravity;
            System.out.println("The gravity is" + gravity + "and total is" + total);
 
 }
}
